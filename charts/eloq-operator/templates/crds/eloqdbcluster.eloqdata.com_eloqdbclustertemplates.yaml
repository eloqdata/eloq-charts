---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    {{- if .Values.keepCrds }}
    "helm.sh/resource-policy": keep
    {{- end }}
    controller-gen.kubebuilder.io/version: v0.16.5
  name: eloqdbclustertemplates.eloqdbcluster.eloqdata.com
spec:
  group: eloqdbcluster.eloqdata.com
  names:
    categories:
    - all
    - eloq
    kind: EloqDBClusterTemplate
    listKind: EloqDBClusterTemplateList
    plural: eloqdbclustertemplates
    shortNames:
    - eloqtemplate
    singular: eloqdbclustertemplate
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Template readiness state
      jsonPath: .status.phase
      name: Ready
      type: string
    - description: Module type
      jsonPath: .spec.tx.module.type
      name: Module
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          EloqDBClusterTemplate is the CRD for defining reusable EloqDBCluster configuration templates.
          Templates are immutable after creation (v1 enforces this via webhook).
          To make changes, create a new template version.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              EloqDBClusterTemplateSpec defines the template for EloqDBCluster configuration.
              This is the complete spec that will be used to create EloqDBCluster instances.
            properties:
              clusterDeployMode:
                description: |-
                  Deprecated: ClusterDeployMode is no longer used to determine workflow behaviour.
                  The operator infers all necessary information from tx/log/module/exposeService spec fields.
                  This field will be removed in a future release. Omit or set to empty string.
                type: string
              googleCloudProject:
                description: |-
                  GoogleCloudProject is the Google Cloud project ID used for GCS-backed storage on GCP.
                  Required when the operator runs on GCP and object storage is used.
                type: string
              log:
                description: |-
                  LogService spec
                  Required for txWithInternalLog mode.
                properties:
                  crossZone:
                    default: false
                    description: CrossZone If configured, the service is deployed
                      across availability zones.
                    type: boolean
                  enableMetrics:
                    default: "on"
                    description: |-
                      Enabled/disabled the metrics.
                      Default: on
                    type: string
                  image:
                    description: Image of the EloqDB component.  (Log/Tx)
                    type: string
                  imagePullPolicy:
                    default: IfNotPresent
                    description: |-
                      PullPolicy for the image, which defaults to IfNotPresent.
                      Default: IfNotPresent
                    enum:
                    - Always
                    - Never
                    - IfNotPresent
                    type: string
                  imagePullSecrets:
                    description: |-
                      (Optional) Secret name containing the dockerconfig to use for a registry that requires authentication. The secret
                      must be configured first by the user.
                    items:
                      description: |-
                        LocalObjectReference contains enough information to let you locate the
                        referenced object inside the same namespace.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    type: array
                  logGroup:
                    default: 1
                    description: |-
                      LogGroup raft group size of LogService.
                      Default: 1
                    format: int32
                    type: integer
                    x-kubernetes-validations:
                    - message: For now LogGroup is immutable
                      rule: self == oldSelf
                  logReplica:
                    default: 1
                    description: |-
                      Number of members in the LogGroup
                      Default: 1
                    format: int32
                    type: integer
                    x-kubernetes-validations:
                    - message: For now LogReplica is immutable
                      rule: self == oldSelf
                  logserverSnapshotInterval:
                    default: 60
                    description: LogserverSnapshotInterval defines the interval for
                      log server snapshots.
                    format: int32
                    type: integer
                  persistentVolume:
                    description: 'Persistent volume to use: EBS'
                    properties:
                      diskType:
                        default: gp3
                        description: |-
                          Cloud provisioned network storage disk type. Must match the cloud provider being used.
                          AWS (EKS) supported types: gp2, gp3, io1, io2, st1, sc1
                          GCP (GKE) supported types: pd-ssd, pd-extreme, pd-standard, hyperdisk-balanced, hyperdisk-extreme
                          Baidu Cloud (CCE) supported types: hp1, ssd, cloud_hp1, premium_ssd, enhanced_ssd_pl1, enhanced_ssd_pl2
                          Default: gp3
                        enum:
                        - enhanced_ssd_pl1
                        - SSD_Enhanced
                        - enhanced_ssd_pl2
                        - enhanced_ssd_pl3
                        - cloud_hp1
                        - premium_ssd
                        - hp1
                        - ssd
                        - gp2
                        - gp3
                        - io1
                        - io2
                        - st1
                        - sc1
                        - pd-ssd
                        - pd-extreme
                        - pd-standard
                        - hyperdisk-balanced
                        - hyperdisk-extreme
                        type: string
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        description: Storage size for the persistent volume claim
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                    required:
                    - size
                    type: object
                  resources:
                    description: Resources of the EloqDB component.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  rpcPort:
                    default: 7001
                    description: |-
                      RPC Port of the LogService.
                      Default: 7001
                    format: int32
                    type: integer
                    x-kubernetes-validations:
                    - message: For now RPCPort of LogService is immutable
                      rule: self == oldSelf
                  schedulePolicy:
                    description: SchedulePolicy Specifies the scheduling policy for
                      the service(TxService/LogService).
                    properties:
                      nodeLabels:
                        description: NodeLabels lists required node labels using AND
                          semantics.
                        items:
                          description: ServiceScheduleNodeLabel is a required key/value
                            node label match.
                          properties:
                            key:
                              description: Key is the node label key.
                              type: string
                            value:
                              description: Value is the node label value.
                              type: string
                          required:
                          - key
                          - value
                          type: object
                        type: array
                      zones:
                        description: |-
                          Zones lists the availability zones where the service may be scheduled.
                          The first zone is treated as the primary zone for single-AZ placement and leader preference.
                        items:
                          type: string
                        type: array
                    type: object
                  store:
                    description: StorageService spec
                    properties:
                      eloqStore:
                        description: |-
                          EloqStore config.
                          Required when storageType is eloqStore. Mutually exclusive with rocksdbCloud.
                        properties:
                          cloudEndpoint:
                            description: Cloud storage endpoint URL
                            type: string
                          dataAppendMode:
                            default: true
                            description: Data append mode
                            type: boolean
                          indexBufferPoolSize:
                            description: Index buffer pool size (e.g., "5GB", "1GB")
                            type: string
                          localSpaceLimit:
                            description: Local space limit (e.g., "2TB", "500GB")
                            type: string
                          manifestLimit:
                            default: 100000
                            description: Manifest limit
                            format: int32
                            type: integer
                          openFilesLimit:
                            default: 260000
                            description: Open files limit
                            format: int32
                            type: integer
                          rangeSliceMemoryLimitPercent:
                            default: 30
                            description: |-
                              Range slice memory limit percent for EloqStore memory management
                              Default: 30
                            format: int32
                            maximum: 100
                            minimum: 1
                            type: integer
                        type: object
                      rocksdbCloud:
                        description: |-
                          Object storage config.
                          Required when storageType is rocksdbCloud. Mutually exclusive with eloqStore.
                        properties:
                          endpointUrl:
                            default: ""
                            description: EndpointUrl Custom S3-compatible endpoint
                              URL (e.g., for MinIO or other S3-compatible storage)
                            type: string
                          fileDeletionDelay:
                            default: 3600
                            description: FileDeletionDelay The file deletion delay
                              for RocksDB Cloud files
                            format: int32
                            type: integer
                          readyTimeout:
                            default: 10
                            description: ReadyTimeout Timeout before RocksDB Cloud
                              becoming ready on new log group leader
                            format: int32
                            type: integer
                          sstFileCacheSize:
                            anyOf:
                            - type: integer
                            - type: string
                            description: SSTFileCacheSize Local sst cache size
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                        type: object
                      storageType:
                        allOf:
                        - enum:
                          - rocksdbCloud
                          - eloqStore
                        - enum:
                          - rocksdbCloud
                          - eloqStore
                        description: Storage service type
                        type: string
                    type: object
                    x-kubernetes-validations:
                    - message: rocksdbCloud config is required when storageType is
                        rocksdbCloud
                      rule: 'self.storageType == ''rocksdbCloud'' ? has(self.rocksdbCloud)
                        : true'
                    - message: eloqStore config must not be set when storageType is
                        rocksdbCloud
                      rule: 'self.storageType == ''rocksdbCloud'' ? !has(self.eloqStore)
                        : true'
                    - message: eloqStore config is required when storageType is eloqStore
                      rule: 'self.storageType == ''eloqStore'' ? has(self.eloqStore)
                        : true'
                    - message: rocksdbCloud config must not be set when storageType
                        is eloqStore
                      rule: 'self.storageType == ''eloqStore'' ? !has(self.rocksdbCloud)
                        : true'
                  txLogRetentionSeconds:
                    default: 180
                    description: |-
                      TxLogRetentionSeconds defines the retention period for transaction logs in seconds.
                      Default: 180
                    format: int32
                    type: integer
                  version:
                    description: Version of EloqDB.
                    type: string
                required:
                - persistentVolume
                - store
                type: object
              region:
                description: Region is the cloud object storage region shared by tx
                  and log storage backends.
                type: string
              s3SecretName:
                description: |-
                  S3SecretName specifies the name of the Kubernetes secret containing S3 credentials (AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY)
                  to be injected as environment variables into the pods for accessing S3-compatible storage services.
                type: string
              serviceAccountName:
                description: |-
                  ServiceAccountName specifies the Kubernetes ServiceAccount used by operator-managed
                  workloads to access cloud resources such as persistent disks, load balancers,
                  and object storage integrations on AWS and GCP.
                type: string
              tx:
                description: |-
                  TxService spec
                  Required for txWithInternalLog mode.
                properties:
                  authentication:
                    description: Authentication indicates whether to enable authentication.
                    type: boolean
                  bootstrap:
                    description: Bootstrap indicates whether to bootstrap the cluster.
                    type: boolean
                  ccStreamPort:
                    default: 8010
                    description: |-
                      CCStream port
                      Default: 8010
                    format: int32
                    type: integer
                    x-kubernetes-validations:
                    - message: For now TxService CCStreamPort is immutable
                      rule: self == oldSelf
                  checkpoint:
                    default:
                      delaySecond: 5
                      intervalSecond: 10
                    description: Checkpoint config of TxService
                    properties:
                      delaySecond:
                        default: 5
                        description: |-
                          The time which ckpt_ts is less than min lock ts case mvcc is enabled. (Unit: second)
                          Default: 5
                        format: int32
                        type: integer
                      intervalSecond:
                        default: 10
                        description: |-
                          Interval time of checkpointer. (Unit: second)
                          Default: 10
                        format: int32
                        type: integer
                    type: object
                  createAdmin:
                    description: |-
                      Deprecated: CreateAdmin is replaced by claim-level control in EloqDBClusterClaim.
                      This field will be removed in a future release. The webhook automatically sets this field to nil.
                    nullable: true
                    type: boolean
                  enableMetrics:
                    default: "on"
                    description: |-
                      Enabled/disabled the metrics.
                      Default: on
                    type: string
                  ephemeralVolume:
                    description: 'Ephemeral volume to use: Local SSD'
                    properties:
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        description: Storage size for the ephemeral volume
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                    required:
                    - size
                    type: object
                  exposedService:
                    description: |-
                      Deprecated: ExposedService is replaced by the top-level exposeService field in EloqDBClusterTemplateSpec.
                      This field will be removed in a future release. Use exposeService instead to control service exposure.
                      The webhook automatically sets this field to nil.
                    nullable: true
                    type: boolean
                  externalIps:
                    description: TxService external Ip
                    items:
                      type: string
                    type: array
                  image:
                    description: Image of the EloqDB component.  (Log/Tx)
                    type: string
                  imagePullPolicy:
                    default: IfNotPresent
                    description: |-
                      PullPolicy for the image, which defaults to IfNotPresent.
                      Default: IfNotPresent
                    enum:
                    - Always
                    - Never
                    - IfNotPresent
                    type: string
                  imagePullSecrets:
                    description: |-
                      (Optional) Secret name containing the dockerconfig to use for a registry that requires authentication. The secret
                      must be configured first by the user.
                    items:
                      description: |-
                        LocalObjectReference contains enough information to let you locate the
                        referenced object inside the same namespace.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    type: array
                  metricPort:
                    default: 18081
                    description: |-
                      Metrics collector Port number
                      Default: 18081
                    format: int32
                    type: integer
                  module:
                    description: User-facing module specification.
                    properties:
                      port:
                        description: |-
                          Port to which the user is connected. This port is currently the connection port for EloqKV or EloqDoc.
                          It is required to be set.
                        type: integer
                      type:
                        allOf:
                        - enum:
                          - eloqkv
                          - eloqdoc
                        - enum:
                          - eloqkv
                          - eloqdoc
                        description: ModuleType for user interaction with eloq.
                        type: string
                    required:
                    - port
                    - type
                    type: object
                  resources:
                    description: Resources of the EloqDB component.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  schedulePolicy:
                    description: SchedulePolicy Specifies the scheduling policy for
                      the service(TxService/LogService).
                    properties:
                      nodeLabels:
                        description: NodeLabels lists required node labels using AND
                          semantics.
                        items:
                          description: ServiceScheduleNodeLabel is a required key/value
                            node label match.
                          properties:
                            key:
                              description: Key is the node label key.
                              type: string
                            value:
                              description: Value is the node label value.
                              type: string
                          required:
                          - key
                          - value
                          type: object
                        type: array
                      zones:
                        description: |-
                          Zones lists the availability zones where the service may be scheduled.
                          The first zone is treated as the primary zone for single-AZ placement and leader preference.
                        items:
                          type: string
                        type: array
                    type: object
                  store:
                    description: StorageService spec
                    properties:
                      eloqStore:
                        description: |-
                          EloqStore config.
                          Required when storageType is eloqStore. Mutually exclusive with rocksdbCloud.
                        properties:
                          cloudEndpoint:
                            description: Cloud storage endpoint URL
                            type: string
                          dataAppendMode:
                            default: true
                            description: Data append mode
                            type: boolean
                          indexBufferPoolSize:
                            description: Index buffer pool size (e.g., "5GB", "1GB")
                            type: string
                          localSpaceLimit:
                            description: Local space limit (e.g., "2TB", "500GB")
                            type: string
                          manifestLimit:
                            default: 100000
                            description: Manifest limit
                            format: int32
                            type: integer
                          openFilesLimit:
                            default: 260000
                            description: Open files limit
                            format: int32
                            type: integer
                          rangeSliceMemoryLimitPercent:
                            default: 30
                            description: |-
                              Range slice memory limit percent for EloqStore memory management
                              Default: 30
                            format: int32
                            maximum: 100
                            minimum: 1
                            type: integer
                        type: object
                      rocksdbCloud:
                        description: |-
                          Object storage config.
                          Required when storageType is rocksdbCloud. Mutually exclusive with eloqStore.
                        properties:
                          endpointUrl:
                            default: ""
                            description: EndpointUrl Custom S3-compatible endpoint
                              URL (e.g., for MinIO or other S3-compatible storage)
                            type: string
                          fileDeletionDelay:
                            default: 3600
                            description: FileDeletionDelay The file deletion delay
                              for RocksDB Cloud files
                            format: int32
                            type: integer
                          readyTimeout:
                            default: 10
                            description: ReadyTimeout Timeout before RocksDB Cloud
                              becoming ready on new log group leader
                            format: int32
                            type: integer
                          sstFileCacheSize:
                            anyOf:
                            - type: integer
                            - type: string
                            description: SSTFileCacheSize Local sst cache size
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                        type: object
                      storageType:
                        allOf:
                        - enum:
                          - rocksdbCloud
                          - eloqStore
                        - enum:
                          - rocksdbCloud
                          - eloqStore
                        description: Storage service type
                        type: string
                    type: object
                    x-kubernetes-validations:
                    - message: rocksdbCloud config is required when storageType is
                        rocksdbCloud
                      rule: 'self.storageType == ''rocksdbCloud'' ? has(self.rocksdbCloud)
                        : true'
                    - message: eloqStore config must not be set when storageType is
                        rocksdbCloud
                      rule: 'self.storageType == ''rocksdbCloud'' ? !has(self.eloqStore)
                        : true'
                    - message: eloqStore config is required when storageType is eloqStore
                      rule: 'self.storageType == ''eloqStore'' ? has(self.eloqStore)
                        : true'
                    - message: rocksdbCloud config must not be set when storageType
                        is eloqStore
                      rule: 'self.storageType == ''eloqStore'' ? !has(self.rocksdbCloud)
                        : true'
                  transaction:
                    default:
                      enableMVCC: "off"
                      isolation: READ-COMMITTED
                      protocol: OCC
                    description: Transaction config of TxService
                    properties:
                      enableMVCC:
                        default: "OFF"
                        description: |-
                          Whether to enable MVCC.
                          Default: OFF
                        type: string
                      isolation:
                        default: READ-COMMITTED
                        description: |-
                          Transaction Isolation Level
                          Default: READ-COMMITTED
                        type: string
                      protocol:
                        default: OCC
                        description: |-
                          Transaction implementation protocol occ or locking.
                          Default: OCC
                        type: string
                    type: object
                  version:
                    description: Version of EloqDB.
                    type: string
                required:
                - authentication
                - bootstrap
                - ephemeralVolume
                - module
                - store
                type: object
            required:
            - serviceAccountName
            type: object
          status:
            description: EloqDBClusterTemplateStatus defines the observed state of
              EloqDBClusterTemplate
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the template's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the generation observed by the
                  controller
                format: int64
                type: integer
              phase:
                description: Phase represents the current phase of the template (Ready
                  or NotReady)
                enum:
                - Ready
                - NotReady
                type: string
            type: object
        required:
        - metadata
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
